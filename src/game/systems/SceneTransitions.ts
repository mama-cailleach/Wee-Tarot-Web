// Phaser handles scene lifecycle (shutdown/create) — no custom manager needed.
// Use scene.scene.start("OtherSceneKey") from any running scene.

export function fadeInIfNeeded(): void {
  // Reserved for fade transitions later.
}
